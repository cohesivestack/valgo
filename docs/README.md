# Valgo Docs (Starlight)

This folder contains the Valgo documentation site built with Astro + Starlight.

## Prerequisites

- Node.js 18+ (recommended: latest LTS)

## Commands

From the `docs/` directory:

```bash
npm install
npm run dev
```

Build / preview:

```bash
npm run build
npm run preview
```
